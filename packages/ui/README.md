# @flowly/ui

Production UI primitives and contextual feedback patterns for Flowly.

- No dependency on `apps/web` or Next.js.
- Public API is exported from `src/index.ts`.
- Import `@flowly/ui/styles.css` once in the application root.
- Components follow DEC-022/023/025 and the interactive `/ui-kit` approval gate.
