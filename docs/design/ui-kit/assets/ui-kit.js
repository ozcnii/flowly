const $=s=>document.querySelector(s),params=new URLSearchParams(location.search),root=document.documentElement,body=document.body;
function setTheme(v){root.dataset.theme=v;$('#theme-toggle').innerHTML=`<svg class="icon"><use href="assets/icons/lucide.svg#icon-${v==='dark'?'sun':'moon'}"/></svg>`;$('#theme-toggle').setAttribute('aria-label',v==='dark'?'Включить светлую тему':'Включить тёмную тему')}
function setSafeArea({top=0,right=0,bottom=0,left=0}={}){for(const[k,v]of Object.entries({top,right,bottom,left}))root.style.setProperty(`--component-safe-area-${k}`,`${Number(v)||0}px`);body.classList.toggle('safe-on',[top,right,bottom,left].some(Number))}
function updateTelegram(webApp){root.dataset.telegram='connected';setTheme(webApp.colorScheme==='dark'?'dark':'light');const p=webApp.themeParams||{},map={bg_color:'--color-canvas',secondary_bg_color:'--color-surface',text_color:'--color-text',hint_color:'--color-text-muted',button_color:'--color-accent',button_text_color:'--color-on-accent'};for(const[k,v]of Object.entries(map))if(/^#[0-9a-f]{6}$/i.test(p[k]||''))root.style.setProperty(v,p[k]);if(Number(webApp.viewportStableHeight)>0)root.style.setProperty('--telegram-viewport-stable-height',`${webApp.viewportStableHeight}px`);setSafeArea(webApp.safeAreaInset)}
function applyTelegram(webApp){if(!webApp)return;updateTelegram(webApp);webApp.onEvent?.('themeChanged',()=>updateTelegram(webApp));webApp.onEvent?.('viewportChanged',()=>updateTelegram(webApp));webApp.onEvent?.('safeAreaChanged',()=>setSafeArea(webApp.safeAreaInset))}
const simulated={colorScheme:params.get('theme')||'light',themeParams:{bg_color:'#f7f6f2',secondary_bg_color:'#fffdf9',text_color:'#24322d',hint_color:'#59655f',button_color:'#36584a',button_text_color:'#ffffff'},viewportStableHeight:844,safeAreaInset:{top:24,right:16,bottom:28,left:16}};
setTheme(params.get('theme')||'light');if(params.get('telegram')==='1')applyTelegram(simulated);else applyTelegram(window.Telegram?.WebApp);if(params.get('safe')==='1')setSafeArea({top:24,right:16,bottom:28,left:16});if(params.get('scale')==='2')root.classList.add('text-200');
$('#theme-toggle').addEventListener('click',()=>setTheme(root.dataset.theme==='dark'?'light':'dark'));$('#safe-toggle').addEventListener('click',()=>setSafeArea(body.classList.contains('safe-on')?{}:{top:24,right:16,bottom:28,left:16}));
const dialog=$('#confirm-dialog');$('#dialog-open').addEventListener('click',()=>dialog.showModal());
function selectExclusive(parent,button,attribute){for(const x of parent.querySelectorAll('button')){x.classList.toggle('active',x===button);x.setAttribute(attribute,String(x===button))}}
for(const group of document.querySelectorAll('.segmented,.chips'))group.addEventListener('click',e=>{const b=e.target.closest('button');if(b)selectExclusive(group,b,'aria-pressed')});
$('.bottom-nav').addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;for(const x of $('.bottom-nav').querySelectorAll('button')){x.classList.toggle('active',x===b);x===b?x.setAttribute('aria-current','page'):x.removeAttribute('aria-current')}});
