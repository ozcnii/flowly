# Этап 2 — Йога

> PRD: §11–19, §43.6–43.12, §43.23, §43.28, §44.2–44.5, §46, §53, §54 этап 2, §55.2.

## Цель

Дать пользователю каталог йоги, поиск и выполнение тренировок, (отложенный) собственный контент и избранное без автоматического подтверждения результата. Ручная запись выполнения — этап 6 (дневник/календарь), см. E6-D7-T09 / DEC-065.

## Сводка

| Backlog | In progress | Blocked | Review | Done |
|---:|---:|---:|---:|---:|
| 0 | 0 | 1 | 0 | 12 |

## Зависимости и инварианты

- Зависит от этапа 1: auth, D1, R2 и UI shell.
- Сессия не завершается автоматически; итоговый статус выбирает пользователь.
- Пошаговый таймер поддерживает `+30 секунд`; избранное без папок. Ручная запись past-date — E6-D7-T09 (не stage 2).

## Обязательные подтверждённые contracts

- По `DEC-024` каждый указанный `ui_slices` screen slice выполняется строго по одному ID в реальном `apps/web`; все states/интеракции и явный approval обязательны до следующего ID.
- По `DEC-035` Konsta UI 5.2.0 (`konsta/react`, `ios` theme) обязательна для current/future production UI; direct imports — default, `packages/ui` допустим только для Flowly-specific contracts, отсутствующих в Konsta.
- По `DEC-028` frontend screen slices нельзя показывать пользователю без самостоятельного UI/UX quality pass по [`docs/design/FRONTEND_REVIEW.md`](../../design/FRONTEND_REVIEW.md): визуальная иерархия, плотность/отступы, релевантность actions, отсутствие служебного текста/fake controls, 360–430px, light/dark, overflow=0, targets ≥44px, console errors=0.
- Home/content errors локальны; YouTube использует cache ≥24h с key по всем filters и явный fallback (`DEC-016`). По `DEC-063` result main-open materialize/reuse private workout через shared service, Play остаётся popup, generated empty detail не показывает fake Exercises/Details, а future reports получают source через occurrence→workout relation.
- Active workout хранит offline checkpoint; status выбирается явно по контекстной матрице (`DEC-015`, `DEC-016`). По `DEC-062` video runtime использует `/sessions/[id]`, one-open D1 constraint, YouTube IFrame API active-time, explicit server/device conflict и atomic occurrence/history finish.
- Первое сохранение своей тренировки требует title, duration, difficulty и executable content; sharing создаёт копию только явным действием (`DEC-016`, `DEC-019`).
- UGC report/hide/block разделены (`DEC-021`); все UI states следуют `DEC-022` и [`docs/design/flows/`](../../design/flows/).

## Deliverable E2-D2 — Каталог и просмотр

### E2-D2-T01 — Подготовить стартовый каталог
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-14
- **prd_refs:** §12.1, §43.6–43.10, §53, §55.2 · **depends_on:** E1-D1-T04, E1-D1-T08 · **decisions:** DEC-010, DEC-029
- **scope:** seed/import категорий, тренировок, упражнений и связей.
- **acceptance:** [x] структура соответствует моделям; [x] импорт повторяем; [x] источники и редакционная проверка фиксируются.
- **validation/evidence:** schema `workout_categories`/`workouts`/`workout_category_links`/`exercises`/`workout_exercises`; migration `migrations/0002_long_spiral.sql`; catalog source `seeds/catalog/starter-catalog.v1.json`; generated SQL `seeds/0002_starter_catalog.sql`; script `scripts/build-starter-catalog-sql.mjs`; counts: users=4, categories=10, workouts=20, exercises=60, category_links=46, workout_exercises=115; sample records queried from local D1.
- **journal:** 2026-07-14 — `backlog -> in_progress`; dependencies E1-D1-T04/E1-D1-T08 done, активных карточек нет. DEC-010 approved: широкий каталог (~10 категорий, 20 тренировок, 60 упражнений), качество подтверждает пользователь по checklist перед `done`. Deep plan `.temp/E2-D2-T01/plan.md` approved пользователем; реализация начата. 2026-07-14 — реализовано: schema + migration + JSON catalog + SQL builder + local-only seed scripts. Проверки PASS: `npm run catalog:build-seed`; `npm run db:reset && npm run db:migrate && npm run db:seed && npm run db:seed:catalog && npm run db:seed:catalog`; counts/sample queries; `npm run typecheck --workspace @flowly/database`; `npm run lint --workspace @flowly/database`; повторный `npm run db:generate` = no schema changes; root `npm run typecheck`/`npm run lint` PASS. E2-D2-T01 `in_progress -> review`. 2026-07-14 — self deep review PASS: 0 блокеров/bugs; catalog consistency audit PASS; DB invariants PASS (`no_category_workouts=0`, `broken_workout_exercise_refs=0`, `source_type=flowly:20`); schema drift/typecheck/lint PASS. Пользователь разрешил закрыть без дополнительных правок; E2-D2-T01 `review -> done`.

### E2-D2-T02 — Реализовать категории, поиск и фильтры
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-14
- **prd_refs:** §11.3, §12.2–12.3, §44.2–44.3 · **depends_on:** E2-D2-T01 · **decisions:** DEC-016, DEC-022, DEC-024, DEC-025, DEC-029
- **ui_slices:** S-MA-020 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** API и UI каталога с определёнными PRD фильтрами.
- **acceptance:** [x] комбинации фильтров работают; [x] empty/loading/error states есть; [x] результаты не выходят за критерии.
- **validation/evidence:** API canonical query set (`base=23`, `source=youtube=3` + фильтры), browser checks 390 light/dark, generated covers, Playwright snapshots/evaluate checks; screenshots через `page.screenshot` в текущей сессии таймаутят.
- **journal:** 2026-07-14 — `backlog -> in_progress`; dependency E2-D2-T01 done, активных карточек нет. Прочитаны stage contracts, DEC-016/022/024/025, S-MA-020 contract и PRD refs. Deep plan `.temp/E2-D2-T02/plan.md` approved пользователем; реализация начата. 2026-07-14 — **S-MA-020 catalog preview implemented**: `GET /api/v1/workouts`, `features/catalog/ui/catalog-screen.{tsx,module.css}`, `features/catalog/model/catalog.ts`, route `?screen=catalog`. Фильтры: search/category/duration/difficulty/format/source/equipment/favorite; source `youtube` и favorite честно дают empty/explanation до downstream tasks. После user feedback UX упрощён: base показывает search + horizontal category chips + compact `Фильтры`, extended filters раскрываются по нажатию. Добавлены 3 стартовые YouTube-тренировки (`BU2iL0mz858`, `o29nP-jH3eA`, `qiKJRoX_2uo`), YouTube thumbnails через `https://i.ytimg.com`, CSP `img-src` обновлён. Проверки PASS: API canonical query set (`base=23`, `source=youtube=3`); browser 390 light/dark base: overflow 0, min target 44px, console errors 0; filter panel opens; YouTube filter shows 3 cards with thumbnail backgrounds; ChatGPT-generated Flowly covers added under `apps/web/public/media/catalog/covers/*.webp`, all 23 catalog cards have real image backgrounds (0 text-only/fake icon covers); separate chevron open button removed, whole card is clickable/keyboard-accessible; card layout changed to top row image+title/meta and full-width details below; empty/error forced states render; web `npm run typecheck --workspace @flowly/web`/`npm run lint --workspace @flowly/web` PASS; `npm run catalog:build-seed` PASS; local D1 `npm run db:seed:catalog` PASS. Screenshot capture в текущей Playwright-сессии таймаутит на `page.screenshot`; evidence — browser snapshots/evaluate + API output. Пользователь approved финальный card UI («кайф мне нравится»); E2-D2-T02 `in_progress -> review`. 2026-07-14 — пользователь отказался от deep review и разрешил закрыть; E2-D2-T02 `review -> done`.

### E2-D2-T03 — Реализовать карточку и страницу тренировки
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-14
- **prd_refs:** §12.4, §13, §44.3–44.4 · **depends_on:** E2-D2-T02 · **decisions:** DEC-016, DEC-021, DEC-022, DEC-024, DEC-025, DEC-028, DEC-029
- **ui_slices:** S-MA-022, S-MA-024, S-MA-088 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** обязательные элементы, упражнения, источник, действия старта/сохранения.
- **acceptance:** [x] все обязательные поля отображены; [x] отсутствующие optional данные безопасны; [x] ownership пользовательского контента соблюдён.
- **validation/evidence:** detail API Flowly/YouTube/404; catalog card click -> detail; author/source profile Flowly=20, YouTube=3, user empty; S-MA-088 report empty validation/report success/hide success/block success; browser 390 light/dark overflow 0, targets >=44px, console errors 0; web typecheck/lint PASS; screenshots via Playwright page.screenshot still timeout in this session, evidence uses snapshots/evaluate/API output.
- **journal:** 2026-07-14 — `backlog -> in_progress`; dependency E2-D2-T02 done, активных карточек нет. Прочитаны roadmap/HANDOFF, карточка E2-D2-T03, stage contracts, DEC-016/021/022/024/025, PRD §12.4/§13/§44.3–44.4, design flows README, S-MA-022/S-MA-024/S-MA-088 contracts and F03/F04/F10 diagrams. Deep plan сохранён в `.temp/E2-D2-T03/plan.md`; пользователь approved план, реализация S-MA-022 начата. 2026-07-14 — реализованы detail API/UI draft и затем UI/UX review corrections по запросу пользователя: detail получил catalog-like padding/max-width, крупные meta-блоки заменены compact summary chips, exercises подняты сразу после hero/summary, противопоказания/источник убраны вниз в `Дополнительно`, actions перенесены после exercises, внутренние roadmap/formulation texts убраны, UGC-only actions скрыты для Flowly/YouTube, fake media placeholders заменены на текстовый step plan с единым честным notice. Проверки PASS: `GET /api/v1/workouts/{id}` Flowly/YouTube/404, catalog card click -> detail, web typecheck/lint, browser 390 light/dark overflow 0, target >=44px, console errors 0. Пользователь approved S-MA-022: «норм пойдет». 2026-07-14 — пользователь попросил доделать оставшиеся slices карточки; начат S-MA-024. Реализован author/source profile route `?screen=author&source=flowly|youtube|user`, список публичных тренировок источника, empty user-content state, safe block/hide explanation without fake mutation for Flowly/YouTube. Проверки PASS: Flowly=20 cards, YouTube=3 cards, user=empty, 390 light/dark overflow 0, targets >=44px, console errors 0, web typecheck/lint. Пользователь approved S-MA-024: «нормальное кайф». 2026-07-14 — пользователь подтвердил старт S-MA-088 report/hide/block; реализация начата. Реализован route `?screen=ugc-safety&action=report|hide|block`: report reason required validation, separate hide outcome, separate block outcome, error/success forced states. Подключены действия из user author profile. После user UI/UX feedback выполнен polish: top nav поднят и уплотнён, header/form объединены визуально в один compact panel, нижние alternate actions стали лёгкими links вместо больших кнопок, service label заменён на пользовательский `Безопасность`, author/safety actions визуально разведены по смыслу (report neutral, hide accent, block danger) и переведены в предсказуемый vertical action list без случайного wrap на 390px. Проверки PASS: report empty validation, report success, hide success, block success, author user links, 390 light/dark overflow 0, target >=44px, console errors 0, web typecheck/lint. Пользователь approved S-MA-088 / продолжение: «ок идем дальше». E2-D2-T03 `in_progress -> review`. 2026-07-14 — пользователь подтвердил закрытие без отдельного deep review: «E2-D2-T03 переводи в done все нормально»; E2-D2-T03 `review -> done`. 2026-07-14 — follow-up polish workout detail: `favorite/share` убраны из нижних bulk actions и перенесены в compact disabled overlay icon buttons на обложку; проверен `?screen=workout&id=wo-yt-malova-vinyasa-24&theme=dark`: overflow 0, targets >=44px, console errors 0, no service text, нижняя action-секция содержит только start.

### E2-D2-T04 — Реализовать YouTube search, cache и save
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-14
- **prd_refs:** §19, §43.28, §44.5, §56.2–56.3 · **depends_on:** E2-D2-T02 · **decisions:** DEC-011, DEC-016, DEC-022, DEC-024, DEC-025, DEC-029, DEC-030, DEC-049
- **ui_slices:** S-MA-021 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** запрос из фильтров, русскоязычные результаты, кэширование и сохранение видео.
- **acceptance:** [x] пользователь может ввести свой YouTube-запрос; [x] input дебаунсится и не отправляет request на каждый символ; [x] query формируется без лишнего `на русском`; [x] cache используется как внутренняя оптимизация без пользовательского `из кэша`; [x] provider unavailable обработан честным state без fake results; [x] quota/provider errors обработаны; [x] save создаёт private copy.
- **validation/evidence:** root/web/youtube typecheck+lint PASS; local D1 migration 0003 applied; historical Invidious verification superseded by DEC-049; production Piped canonical/edge HTTP verification recorded in journal; HTTP search returns real results and normalized `i.ytimg.com` thumbnails; debounce verified; unavailable state returns controlled HTTP 200/no fake results; HTTP save create/idempotent/401/403 PASS; browser S-MA-021 390 dark/light checks PASS; React Query raw fetch audit PASS.
- **journal:** 2026-07-14 — `backlog -> in_progress`; dependency E2-D2-T02 done, E2-D2-T03 done, активных карточек нет. Пользователь поручил начать E2-D2-T04 после push. Начат required workflow: roadmap/HANDOFF/card прочитаны; далее читать PRD refs, DEC-011/016/022/024/025/029, design contracts S-MA-021/F03, подготовить deep plan `.temp/E2-D2-T04/plan.md` до кода. 2026-07-14 — PRD §19/§43.28/§44.5/§55.2, S-MA-021/F03, traceability и current schema/API прочитаны; пользователь выбрал Invidious provider без `YOUTUBE_API_KEY` и approved plan v1 scope (`INVIDIOUS_BASE_URL`, dev fallback, save private workout, create-workout disabled до editor, unsuitable UI-local). DEC-030 added; реализация начата. 2026-07-14 — implemented draft: `youtube_search_cache` schema/migration, `@flowly/youtube` Invidious-compatible provider, `GET /api/v1/youtube/search`, `POST /api/v1/youtube/videos/:videoId/save`, React Query hooks, S-MA-021 `?screen=youtube`, catalog entry `Найти видео`, privacy filter for saved private workouts. Проверки PASS: root typecheck/lint, raw client fetch audit (only `apps/web/lib/api/client.ts`), local DB migration 0003, HTTP cache repeat hit and changed filter different cache key, save create/idempotent/401/403, browser 390 dark S-MA-021 overflow 0 targets >=44 console errors 0 no service text, `Не подходит` hides result locally. 2026-07-14 design-review fixes: forced error/empty states fixed; first card starts at y≈261 on 390px; repeated language warning replaced by one compact note; raw query moved to `Как искали` details below results; disabled `Создать` removed; thumbnail fixed-size; actions hierarchy changed to primary save + secondary watch + tertiary unsuitable. 2026-07-14 user rejected remaining layout/copy: removed `Не подходит`, removed visible cache state, removed `на русском` from query builder, bumped cache key to v2, disabled dev fixture as real-results fallback, converted card to catalog-like layout (`thumb+head` top, details/actions below full width). 2026-07-14 user selected `Input + фильтры`: added free YouTube search input with 350ms debounce, `query/q` API param and query-key support; verified arbitrary query text (`пилатес для шеи до 10 минут`) without `на русском`, browser debounce only one extra search after typing, mocked visual result no cache/unsuitable text. Routing cleanup decision deferred until after E2-D2-T04 as DEC-031. 2026-07-14 user review fixes: selected category now prefills input (`Йога Спина`) instead of showing category below; YouTube result cards changed to YouTube-like layout with large 16:9 thumbnail, duration overlay, title/channel/views/date, Flowly save/watch actions; unavailable descriptions hidden instead of showing `Описание недоступно`; thumbnails normalized to `https://i.ytimg.com`. Verified 390 dark: 12 real cards, first thumb 327x184, overflow 0, console errors 0, no `Описание недоступно`, no category line below input, no `на русском`. Пользователь approved S-MA-021: «кайф давай дальше чо там у нас». E2-D2-T04 `in_progress -> review`; residual risk: public Invidious instance is best-effort dev verification and not production contract, provider should be configured per environment. 2026-07-15 — DEC-049 supersedes provider only: production Invidious repro HTTP 200/unavailable/0 replaced by Piped commit `286d597`; Deploy web 29402655934 PASS; exact rerun HTTP 200/miss/12 then hit/12, three edge queries return 12/10/6 with warning null; cache/stale/save contracts preserved. 2026-07-14 — пользователь явно отклонил deep review («не надо и так заебись») и поручил идти дальше к routing cleanup по DEC-031; E2-D2-T04 `review -> done` без deep review.

### E2-D2-T08 — Открывать YouTube-результат как тренировку
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-18
- **prd_refs:** §14–15, §19.3, §28, §30, §44.5 · **depends_on:** E2-D2-T04, E2-D3-T01 · **decisions:** DEC-029, DEC-035, DEC-053, DEC-054, DEC-062, DEC-063
- **ui_slices:** S-MA-021, S-MA-022 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** card main-open → idempotent private YouTube workout → shared detail/session; generated empty detail без Exercises/Details; occurrence→workout source mapping для E6 report Badge `YouTube`.
- **acceptance:** [x] Play не материализует workout; [x] main-open/save concurrent idempotent и ведут на один detail ID; [x] generated detail переиспользует shared UI/runtime без fake sections; [x] Finish создаёт canonical occurrence/history; [x] occurrence report mapping возвращает YouTube source; [x] E6 UI scope не реализуется частично.
- **validation/evidence:** plan `.temp/E2-D2-T08/plan.md` approved; migration `0008_clumsy_stingray.sql` local PASS (index `workouts_owner_youtube_video_unique` applied). HTTP `.temp/E2-D2-T08/{concurrent-materialization,http-lifecycle,http-errors}.json`: concurrent Save/Create = 200 created true/false same ID; repeats same ID; auth401/origin403/bad ID400; generated detail source YouTube/exercises0/Start enabled; Start→Finish occurrence/history PASS. D1 join occurrence→workout returns `source_type=youtube`; per-owner row count=1. Muted generated-detail matrix 360/390/430: overflow0, targets≥44, Start visible, Exercises/Details absent, console errors0; screenshot `/tmp/flowly-youtube-generated-detail.png`. Seeded YouTube fixture retains 5 exercises. Root typecheck/lint/build PASS.
- **independent re-verification (2026-07-18, текущий агент):** dev server `:3002` с dev-emulation user `019f6361`. Concurrent real-video Save+Create-workout → один ID, `created` true на fresh видео; repeat → `created=false` тот же ID. Generated detail API: `sourceType=youtube`, `format=video`, `exercises.length=0`, `actions.start.enabled=true`. Browser `/youtube` 390px: каждая карточка имеет ровно 3 действия — «Открыть» (materialize overlay), «Воспроизвести» (DEC-053 popup, не создаёт workout), «Сохранить»; click «Открыть» → `/workouts/<new-id>` без reload. Generated detail DOM: `hasExercises=false`, `hasDetails=false`, `Начать тренировку` enabled, Hero Play visible, doc/body overflow=0, console errors=0. Сравнение с user-reference `/workouts/019f6a1f-...`: тот же самый screen, тот же YouTube-source metadata `YouTube · Видео · 31 мин · Лёгкая`, тот же Hero/ActionPanel — UI идентичен. Start→Finish на materialized YouTube workout создал occurrence `019f75d4-...`; SQL `activity_occurrences JOIN workouts ON workouts.id=entity_id WHERE source_type='youtube'` возвращает `source_type=youtube` — report-source data contract подтверждён.
- **residual risks:** production duplicate preflight (partial unique index по owner+source+youtube_video_id): если в prod уже есть дубликаты, remote migration 0008 упадёт — CI deploy безопасно прерывается без partial deploy (старый save имел pre-check, дубликаты маловероятны); visible report UI (text Badge `YouTube`) корректно остаётся E6 — report-экраны пока backlog; real Telegram WebView autoplay/player остаётся device-check.
- **journal:** 2026-07-17 — пользователь выбрал card→detail с отдельной Play action, materialization при первом open, report data contract сейчас/full UI в E6 и отдельный E2-D2-T08 plan. Добавлен DEC-063; card создана `blocked`. Пользователь approved plan и явно закрыл dependency E2-D3-T01 как `done`; выполнен переход `blocked -> in_progress`. Реализованы shared materialization service, Save/Create routes, DB unique index, card main-open и shared generated-detail cleanup; проверки PASS, deploy ждёт UI approval. 2026-07-18 — текущий агент перепроверил реализацию по просьбе пользователя («проверь что все корректно и если все ок закрывай задачку»): re-verification HTTP materialization idempotency, generated-detail DOM-matrix, /youtube 3-action card, occurrence→workout report-source join, UI-идентичность с user-reference workout, root typecheck/lint/build — всё PASS. Пользователь явно разрешил commit+push и закрытие; E2-D2-T08 `in_progress -> review -> done`.

### E2-D2-T06 — Routing cleanup product paths
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-15
- **prd_refs:** §44.2–44.5 · **depends_on:** E2-D2-T04 · **decisions:** DEC-024, DEC-028, DEC-029, DEC-031, DEC-032
- **ui_slices:** existing approved screens only — no new product UI slice; routing/navigation behavior cleanup.
- **scope:** заменить пользовательские `/?screen=...` entrypoints и links на product route paths (`/catalog`, `/youtube`, `/workouts/[id]`, `/authors/[source]`, `/profile`, `/settings`, `/help`, `/safety/...`) без legacy `screen` redirects; временно оставить dev-only query params для theme/scenarios/forced states с последующим долгом на удаление.
- **acceptance:** [x] product routes открывают те же approved screens; [x] internal links/router.push используют route paths; [x] `?screen=...` больше не используется как product routing; [x] dev-only forced params остаются работоспособны; [x] React Query/API behavior не меняется; [x] browser checks на ключевых routes pass.
- **validation/evidence:** `npm run typecheck --workspace @flowly/web` PASS; `npm run lint --workspace @flowly/web` PASS; `rg "?tab=|tab=|params.tab|screen=|params.screen|?screen="` audit PASS in `apps/web/app`, `components`, `features`; shared route group `apps/web/app/(app)/layout.tsx` + `AppRouteShell` preserve one `AuthGate/AppShell`; browser route matrix PASS for `/`, `/catalog`, `/youtube?q=йога&duration=short`, `/workouts/wo-yt-malova-vinyasa-24`, `/authors/youtube`, `/profile`, `/settings`, `/help`, `/safety/report`, `/programs`, `/rhythm`, `/calendar`; bottom-nav click matrix PASS with same shell DOM node, nav hrefs paths-only, console errors 0, overflow 0, no auth error, no visible dev copy; legacy `/?screen=catalog` no longer opens catalog.
- **journal:** 2026-07-14 — создана новая card после E2-D2-T04 done по user instruction «давай routing cleanup по DEC-031»; пользователь выбрал: plan file `.temp/routing-cleanup/plan.md`, новая card, без legacy redirects, dev query params временно оставить и позже выпилить. `backlog/new -> in_progress`; plan saved to `.temp/routing-cleanup/plan.md`; user approved: «делай». Implemented route pages `/catalog`, `/youtube`, `/workouts/[id]`, `/authors/[source]`, `/profile`, `/settings`, `/help`, `/safety/[action]`; removed root `params.screen` product router; replaced product links/router.push. Verification PASS; E2-D2-T06 `in_progress -> review`. 2026-07-15 follow-up: user asked for Telegram-less testing and reviewed ugly auth error screen; enabled existing dev emulation flow end-to-end (`FLOWLY_DEV_EMULATION=1` from `.dev.vars`, local `GET /me` can create mock session), fixed encoded dev-user header, polished AuthGate error/loading card with safe side margins. Verified clean-cookie `/` logs into mock user without Telegram, overflow 0, console errors 0; forced `?auth=error` has 16px side margins, target >=44, console errors 0. 2026-07-15 user escalated systemic UX/routing issues: navbar disappeared on route pages, bottom nav used `?tab=...`, inconsistent back placement, navigation looked like re-auth/full reload flicker, padding doubled. Added DEC-032 and updated `docs/design/FRONTEND_REVIEW.md`/`AGENTS.md`; E2-D2-T06 must undergo detailed UX/architecture review and fixes by DEC-032 before done. 2026-07-15 DEC-032 fix implemented: product routes moved into shared Next route group `app/(app)` with persistent `layout.tsx`; `AppRouteShell` owns single `AuthGate + AppShell`; individual route pages no longer wrap standalone shell; obsolete `RouteShell` removed; bottom nav switched to client-side `router.push` so clicks do not document-reload/remount shell; nav hrefs are paths only; visible dev scenario label hidden. Verification PASS: web typecheck/lint, query-route audit, browser click matrix same shell DOM node, route matrix nav/header visible on all product paths, overflow 0, console errors 0, no auth error, no visible dev copy. 2026-07-15 settings UX follow-up: bottom `Сохранить`/`Отмена` removed; settings now autosave changes with 550ms debounce and compact `Сохраняем…/Сохранено` status; service copy about future stages removed. Verification PASS: `/settings` 390px no save/cancel buttons, autosave status appears after input change, bottom nav visible, global header absent, overflow 0, console errors 0, web typecheck/lint PASS. 2026-07-15 — пользователь подтвердил, что E2-D2-T06 уже сделана; acceptance/evidence заполнены, unresolved blockers отсутствуют, `review -> done`.

### E2-D2-T07 — Enforce UI consistency primitives
- **status:** done · **priority:** blocker · **owner:** AI agent · **updated:** 2026-07-15
- **prd_refs:** §44.2–44.5, §55 · **depends_on:** E2-D2-T06 · **decisions:** DEC-024, DEC-028, DEC-032, DEC-033
- **ui_slices:** existing product screens only; no new product scenario. Scope is visual/system consistency for already implemented screens.
- **scope:** зафиксировать единый набор UI primitives/tokens для всех product screens и убрать произвольные отличия в page padding, top/back placement, card/list spacing, colors, avatar/icon sizes and local arbitrary values. Считать проблему глобальной для всех страниц, пока audit не докажет обратное. Прогнать весь `apps/web/features/**/ui` и route-level pages; привести все текущие product routes (`/`, `/profile`, `/settings`, `/help`, `/catalog`, `/youtube`, `/workouts/[id]`, `/authors/[source]`, `/safety/[action]`, `/programs`, `/rhythm`, `/calendar` и любые найденные route-accessible screens) к одному contract. Preview-only onboarding/recovery/web fallback screens либо включить в этот pass, либо явно вынести в follow-up с причиной. Обновить `AGENTS.md`, `docs/design/FRONTEND_REVIEW.md`, `DECISIONS.md`, карточку и HANDOFF так, чтобы будущие агенты не могли вводить произвольные локальные spacing/color/layout primitives без approval.
- **acceptance:** [x] DEC-033 approved/recorded; [x] shared screen and typography primitives/tokens defined; [x] all product screens use the same page/screen/top/back/title/subtitle/card/list primitives where applicable; [x] sibling screens have identical page edge/top/back geometry; [x] global audit finds and documents arbitrary spacing/color/radius/font drift across `apps/web/features/**/ui`; [x] repeated local screen/top/back values replaced with approved tokens/classes or explicitly justified as content-specific; [x] `/profile -> /settings -> /help` has no visible position jump for page edge/back/top; [x] catalog/youtube/workout/author/safety and bottom-nav routes have no arbitrary geometry drift in measured shell primitives; [x] product route matrix keeps nav/shell stable; [x] governance docs updated.
- **validation/evidence:** `.temp/ui-consistency-system/plan.md`; DEC-033 in `docs/roadmap/DECISIONS.md`; primitives in `apps/web/app/globals.css`; governance in `AGENTS.md` and `docs/design/FRONTEND_REVIEW.md`; CSS drift audit: product `.screen` blocks are empty, media screen overrides removed, top/back geometry owned by `.flow-*`; typography audit: route-accessible product titles use `.flow-title` with computed 36px / 40.32px line-height / `Cormorant Garamond`, eyebrow uses `.flow-eyebrow` 12px, subtitles use `.flow-subtitle` 14px on `/`, `/catalog`, `/youtube`, `/workouts/*`, `/authors/*`, `/safety/*`, `/profile`, `/settings`, `/help`, `/programs`, `/rhythm`, `/calendar`; mobile density audit: Home no longer compounds `.flow-screen` gap with local block margins (`gapCatsProgram=16`, `gapProgramHabits=16`, `gapActionMore=16` at 390px), category chips wrap without horizontal strip overflow, catalog thumbnails visible on mobile (`cover=100x100` at 390px) and catalog chips have `overflow=0`; back control width audit PASS after `.flow-back { align-self:flex-start }`: `/youtube`, `/workouts/*`, `/authors/*`, `/safety/report`, `/profile`, `/settings`, `/help` back widths are content-sized (91–111px), x/y consistent, overflow 0; `npm run typecheck --workspace @flowly/web` PASS; `npm run lint --workspace @flowly/web` PASS; query-route audit PASS; browser matrix 390px PASS for `/`, `/profile`, `/settings`, `/help`, `/catalog`, `/youtube?q=йога&duration=short`, `/workouts/wo-yt-malova-vinyasa-24`, `/authors/youtube`, `/safety/report`, `/safety/hide`, `/safety/block`, `/programs`, `/rhythm`, `/calendar`: overflow 0, console errors 0, targets >=44, nav visible, no global header, no auth/dev copy, same shell on bottom-nav clicks. 360px PASS after catalog chip strip token fix; 430px PASS for profile/settings/help/catalog/youtube/workout measured set. Home Telegram-WebView follow-up: direct `.flow-screen` children cannot shrink (`flex-shrink:0`), screen margin is horizontal-only, Home sections use shared 12px internal gap, and a renamed `home-screen-v2.module.css` produces a new CSS asset instead of reusing Telegram's stale cached module; 360/390/430 measured gaps are exactly 16px between top-level blocks, overflow 0. Latest shell/catalog follow-up: horizontal catalog cards and icon-only filter approved for local production test; favorite controls on catalog/detail share one disabled `bookmark` component and 44×44 target. Fullscreen inset initial emulation used an incorrect shared max; real iPhone screenshot reproduced Telegram controls overlap. Official Telegram diagrams confirm nested insets; corrected verification PASS: system top=59px + content top=46px gives shell top=105px and first content y=125px; bottom=34px produces main reserve=122px, navbar fixed height=64px with bottom≈42px, and final catalog card clears navbar by≈32px; UI/web typecheck and targeted lint PASS; console errors 0.
- **journal:** 2026-07-15 — created from user feedback: `/profile` vs `/settings` page padding/back placement visually jumps; arbitrary screen CSS values are unacceptable. User clarified this likely exists on all pages and must be treated globally, not only on reported screens. Plan saved/updated at `.temp/ui-consistency-system/plan.md`. User approved implementation («делай»); E2-D2-T07 `backlog -> in_progress`. Implemented app-level primitives in `apps/web/app/globals.css` first because this fixes current product screens immediately without expanding `packages/ui` public API; package extraction can be a later explicit decision if needed. Added DEC-033 and governance docs. Connected `flow-screen`, `flow-top`, `flow-back`, `flow-card`, `flow-list-row`, `flow-status` to route-accessible product screens and preview shell screens where applicable; removed local product `.screen/.top/.back` geometry and media screen overrides; fixed 360px catalog overflow by tying chip strip to `--flow-screen-gutter`. Verification PASS; E2-D2-T07 `in_progress -> review`. 2026-07-15 Home design review pass: user approved `Today focus` direction. Fixed global bottom whitespace by changing `--flow-screen-pad-bottom` from nav-height reserve to `var(--primitive-space-4)` because sticky bottom nav already occupies layout space; removed empty Home live-region when no notice; removed Home category chips; moved primary `Начать тренировку` into first viewport; converted program hero to compact secondary card; removed social friend activity from Home; kept secondary recommendations below Today focus. Verification PASS: `npm run typecheck --workspace @flowly/web`, `npm run lint --workspace @flowly/web`; browser 360/390/430 Home overflow 0, console errors 0, min target 44, bottom gap to nav 16px, primary action visible in first viewport, scroll height reduced from ~2.5 to ~2.1 screens. Follow-up after avatar screenshot bug report: Home profile entry no longer renders as a bare `А`; replaced with visible circular accent avatar button using `user-round` icon. Verification PASS: `/` and `/?theme=dark` at 390px show 44x44 profile link at top right with 22x22 icon, overflow 0, min target 44. 2026-07-15 Catalog design review pass: reduced pre-list controls from 3 category rows + separate YouTube row to one quick-chip row and moved full category list into filter panel; moved YouTube action into result bar as secondary action; restored mobile thumbnails and kept chips wrap without horizontal strip. Verification PASS: web typecheck/lint; `/catalog` 360/390/430 overflow 0, min target >=44, quick chips height 44/no horizontal overflow, first card y≈308 instead of y≈446, thumbnails visible (`88–100px`), filters redesigned as bottom sheet: groups `Категория`, `Длительность`, `Сложность`, `Формат`, `Дополнительно`, draft changes apply only on `Готово`, `Сбросить` clears draft; filter count includes category + all extra filters. Follow-up: removed duplicate quick category row from main page; removed inline reset to prevent layout jumps; drawer uses fixed header/footer with only middle body scrolling. Verification PASS: drawer 390px overflow 0, targets >=44, groups visible, header/footer y positions unchanged while body scrolls, selecting `Спина` + `11–20 мин` gives `Фильтры · 2` and 4 results, reset clears active filters, main page search block height and first card y stay stable before/apply/reset (`searchH=46`, `firstCardY=255`). Catalog card follow-up: replaced bulky square-card layout with compact media-row cards: 16:10 thumbnails, 1-line description clamp, max 3 tags; fixed result copy with Russian declension (`Найдено 23 тренировки`). Follow-up after screenshot review: media-row approach rejected because it still left awkward lower-left empty space; catalog cards switched to vertical banner layout with full-width 16:10-ish thumbnail, compact metadata, 1-line description and max 3 tags. Result bar changed from split left/right layout to compact inline count + secondary YouTube action. Extra bottom whitespace removed by hiding empty live-region and preventing product `.flow-screen` from adding a second viewport height inside persistent shell. Follow-up polish after screenshots: removed catalog subtitle to reduce text load; result bar now shows only count (`23 тренировки`) and no visually competing YouTube action; product empty explanation no longer mentions roadmap/stage; card thumbnail height increased; filter drawer body gets bottom scroll padding so last chip row is fully visible above fixed footer. Follow-up: thumbnail height increased again to make cards less flat, and YouTube search returned as calm inline `· Искать видео` action next to the count instead of a large competing button. Final catalog image follow-up: cover now uses pure `aspect-ratio: 16 / 9` like YouTube cards, no manual height. Theme bug follow-up: explicit theme settings persisted via `localStorage` (`flowly-theme=light|dark|system`) and root bootstrap applies saved explicit theme before Telegram/system fallback. Started `/youtube` page review: removed subtitle, removed visible search label and technical language note, clamped result titles to 2 lines. YouTube result cards aligned with approved catalog rhythm: same vertical 16:9 preview card, compact `YouTube` + duration pills, title clamp, channel/meta one-line, save/watch actions below. Follow-up after screenshot review: removed broken double-frame search card; search now matches catalog-style single pill with search icon, and card internals got more breathing room (larger padding/gaps between preview, pills, title, channel, meta and actions). Verification PASS: 360/390/430 overflow 0, min target >=44; catalog first card y≈223 at 390, first card height ~308px, 3 cards visible in first viewport, thumbnail 327x184 at 390 (16:9); theme `Светлая` reload remains light from `?theme=dark`; `/youtube` search block reduced 92px->46px after final single-pill search, first result y≈223 instead of y≈331, first YouTube card 390px: 350x393 with 316x178 thumbnail and 2 cards visible in first viewport; dark theme PASS with overflow 0 and min target 44. 2026-07-15 Telegram production screenshot follow-up: Home profile entry now consumes cached React Query `/me.photoUrl` and renders the current Telegram `t.me/i/userpic/**` photo inside the 44px circular link, with `user-round` fallback; CSP/Next image allowlist is restricted to that Telegram userpic path. Fixed WebView-only vertical compression by preventing direct `.flow-screen` children from shrinking, normalized section gap to shared token, removed vertical auto margins, and forced a fresh CSS module asset via `home-screen-v2.module.css`. Verification: fresh current-HEAD dev server, 360/390/430 DOM measurements show overflow 0, top-level gaps `[16,16,16,16,16,16]`, sections `flex-shrink:0`, avatar 44x44/radius 50%/overflow hidden; web typecheck PASS; targeted ESLint PASS; production build PASS and generated new CSS asset `1lnn4ocqof_yw.css`. 2026-07-15 latest user follow-up supersedes the earlier rejected media-row note for the current candidate: catalog cards changed back to the newly approved compact horizontal image/text layout, filter is an icon-only funnel with count badge, shared disabled favorite uses `bookmark` in both catalog/detail, and Home greeting leaf was removed. Implemented hybrid Telegram/iOS safe areas and a 64px floating glass navbar with safe-area offset plus AppShell-wide bottom content reserve; `requestFullscreen()` is gated to Telegram API >=8.0 so older clients use `expand()` without SDK console errors; emulated top/bottom/last-content geometry and typecheck/lint passed. Commit `f65b287` pushed; GitHub Actions run `29382506441` PASS, production `/`, `/catalog`, `/workouts/wo-balance-basic-15` health=200; real iPhone rerun exposed top overlap: shared `max(safe, content-safe)` undercounted nested Telegram controls. Official `SafeAreaInset`/`ContentSafeAreaInset` diagrams confirm additive geometry; tokens corrected to `system-safe + content-safe`, clean-server computed verification gives shell top 105px and first content y=125px for 59px/46px runtime values. Fix commit `1d790e5`, GitHub Actions run `29382959191` PASS; пользователь повторил тот же production fullscreen scenario на real iPhone и подтвердил: «работает». 2026-07-15 — пользователь подтвердил, что E2-D2-T07 уже сделана; acceptance/evidence и real-device approval присутствуют, unresolved blockers отсутствуют, `review -> done`.

### E2-D2-T05 — Реализовать избранное
- **status:** done · **priority:** normal · **owner:** AI agent · **updated:** 2026-07-19
- **prd_refs:** §18, §43.12, §55.2 · **depends_on:** E2-D2-T03 · **decisions:** DEC-022, DEC-024, DEC-025, DEC-029, DEC-035, DEC-053
- **ui_slices:** S-MA-023 (+ live favorite controls on S-MA-020/022/025 wiring).
- **scope:** добавить, удалить и показать единый список без папок; entity stage-2 = `workout` (Flowly + materialized YouTube); YouTube search bookmark = materialize + favorite toggle.
- **acceptance:** [x] операции идемпотентны; [x] список приватен владельцу; [x] folders отсутствуют.
- **validation/evidence:** plan `.temp/E2-D2-T05/plan.md`; migration `0010_favorites.sql`; HTTP matrix `.temp/E2-D2-T05/http/**`; commit `fad6bc2` pushed; user UX pass (filled/outline toggle, YouTube→`/favorites`, no chevron on detail row).
- **journal:** 2026-07-19 — implement + UX fix + push `fad6bc2`. User: close as done with stage DoD.

## Deliverable E2-D3 — Выполнение и создание тренировок

### E2-D3-T01 — Реализовать видеосессию
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-17
- **prd_refs:** §14.1, §14.4, §15, §43.21, §43.23–43.24 · **depends_on:** E2-D2-T03 · **decisions:** DEC-015, DEC-016, DEC-022, DEC-024, DEC-029, DEC-035, DEC-062
- **ui_slices:** S-MA-012, S-MA-030, S-MA-033, S-MA-034 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** persisted `/sessions/[id]` start/resume/finish lifecycle, YouTube IFrame API active-time, one-open conflict, server+offline checkpoint, explicit final status и minimal occurrence/history persistence.
- **acceptance:** [x] открытая сессия сохраняется; [x] автозавершения нет; [x] итог подтверждает пользователь.
- **validation/evidence:** plan `.temp/E2-D3-T01/plan.md`; migration `0006_cool_dreadnoughts.sql` local PASS; canonical HTTP evidence `.temp/E2-D3-T01/http/**`: start 201, duplicate/new conflict 409, non-executable 409, checkpoint 200, stale 409, explicit force 200, missing status 400, finish/repeat 200 with same occurrence, five-status matrix PASS, occurrence/history exactly 1. Browser evidence `/tmp/flowly-session-review-shots/session-390-dark.png`: 360/390/430 overflow 0, visible targets ≥44, Tabbar 0, controlled `youtube-nocookie` IFrame API params, Home real resume, historical final Dialog no-default state, Start conflict Continue/Close/Cancel, console errors 0. Root typecheck/lint/build/OpenNext deploy-check PASS. User screenshot review rejected current 3-button conflict Dialog, final-status Dialog and responsive exercises spacing. Canonical bug repro `.temp/E2-D3-T01/checkpoint-repro.json`: after actual `0:03` IFrame playback and SPA Back, local checkpoint=0 and server=0; exact progress loss confirmed. Correction plan/user approval and real-device Telegram player review pending.
- **journal:** 2026-07-16 — после закрытия E0-D0-T05 пользователь выбрал E2-D3-T01 как следующую карточку. Dependency E2-D2-T03 `done`; прочитаны PRD refs, DEC-015/016/022/024/025/029 и superseding DEC-035, F04/S-MA-012/030/033/034 contracts. Выполнен переход `backlog -> in_progress`; до plan approval runtime code не менять. 2026-07-17 — пользователь выбрал plan file `.temp/E2-D3-T01/plan.md`, подтвердил `/sessions/[id]`, IFrame API, occurrence persistence и server+offline checkpoint, затем approved implementation («да делай»); создан DEC-062. Реализованы schema/API/client/runtime/Home resume/detail conflict/navigation/player integration; local migration, canonical HTTP и focused browser checks PASS. Card остаётся `in_progress` до full build/deploy-check, user slice approval и real-device Telegram player check. 2026-07-17 — user review requested: replace cramped conflict UI; same-workout active detail must show current marker/Continue and route directly without modal; redesign finish UI; stabilize exercise spacing across widths; fix lost short playback/technical offline checkpoint UX; confirm when E2-D3-T02 non-video sessions follow. Existing implementation is not approved. Actual pre-fix repro captured 0:03→SPA Back→local/server 0. Post-fix muted controlled rerun `.temp/E2-D3-T01/checkpoint-postfix.json`: 0:03→local/server 3→restored 0:03; Pause keeps iframe Y/height and Card height exact while status changes only `Идёт тренировка`→`Пауза`. Home resume card compacted and uses shared unbounded `m:ss`/`h:mm:ss` active elapsed. Browser and mocked native Back exact prior-detail→Home→Continue→session→Back both return `/` with history index 1→0; reported workout-detail return not reproduced without the exact pre-Home sequence. Пользователь выбрал: other-active bottom Sheet; same-active Badge/time/direct Continue; final bottom Sheet с default `completed` и раскрываемым comment; duration под exercise title; unbounded PLAYING elapsed formatted `m:ss`/`h:mm:ss`; E2-D3-T02 сразу после acceptance T01. `.temp/E2-D3-T01/plan.md` обновлён до correction v2 (Plan confidence 96%, Implementation confidence 93%); пользователь явно re-approved. Corrections реализованы: StrictMode-safe monotonic restore, stable Pause, human sync copy, compact Home/full elapsed, same-active direct Continue, vertical conflict/final Sheets, default `completed`, responsive exercises. Final root typecheck/lint/build/OpenNext deploy-check/diff-check PASS. Карточка оставалась `in_progress` до user/real Telegram approval и уточнения невоспроизведённой browser-Back preceding sequence. User correction v3 explicitly approved: отдельно сохранять/seek-ить YouTube playback second, не смешивая её с active elapsed; при server/device elapsed delta `<1s` автоматически брать server и не показывать sync Sheet. Implemented migration `0007_married_wrecking_crew.sql`, schema/API/local snapshot/finish/player `seekTo`. HTTP evidence `.temp/E2-D3-T01/http/playback-{position,finish}.json`: start position0, checkpoint elapsed12/position37→position38, stale409 exposes position38, idempotent finish keeps position38/one occurrence. Muted browser `.temp/E2-D3-T01/playback-restore.json`: stale token/equal elapsed auto-server, dialog false, timer0:12, local rebase position38, `seekTo(38)`. Rolling-deploy compatibility `.temp/E2-D3-T01/http/playback-backward-compat.json`: old checkpoint/finish bodies without playback field remain 200 and preserve position44. Plan confidence 97%, implementation confidence 92%. Commit `b69e190` pushed; production deploy run `29579333079` PASS with remote migrations 0006/0007 and worker deploy. Production browser-UA smoke `/`=200, `/sessions/smoke`=200, unauth active API=401. Real iPhone final-Sheet follow-up: bottom controls nearly touched rounded edge and horizontal scroll appeared. Fix uses `pb-safe-6`, bounded/wrappable comment action, max-width/X clipping. Muted 200% `.temp/E2-D3-T01/sheet-spacing.json`: document/body/Sheet overflow 0, footer bottom gap 48px at doubled scale, console errors 0. Follow-up commit `85b9cc4`, production deploy run `29582336030` PASS. 2026-07-17 — пользователь явно выбрал «Закрыть как done» после production/device feedback; acceptance подтверждён, E2-D3-T01 `in_progress -> review -> done`. Browser-Back exact preceding sequence не воспроизведена и остаётся documented residual, не hidden blocker.

### E2-D3-T02 — Реализовать пошаговую и смешанную сессию
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-19
- **prd_refs:** §14.2–14.4, §15 · **depends_on:** E2-D2-T03, E2-D3-T01 · **decisions:** DEC-015, DEC-016, DEC-022, DEC-024, DEC-025, DEC-029, DEC-035, DEC-053, DEC-062
- **ui_slices:** S-MA-031 (step — shipped); S-MA-032 (mixed chooser — **deferred residual**, not required for close); S-MA-012/033/034 regression-only.
- **scope:** шаги, таймер, pause/resume, `+30 секунд`, step media/VO/rest UX; mixed chooser S-MA-032 явно отложен.
- **acceptance:** [x] переходы шагов корректны; [x] +30 не ломает состояние; [x] mixed flow — deferred (S-MA-032 out of this close, user approved S-MA-031 scope); [x] статус не автоматический.
- **validation/evidence:** plan `.temp/E2-D3-T02/plan.md`; migration `0009`; `StepSessionScreen` intro→exercise→rest, VO, square MP4 media, done-screen actions; production deploy `e5b55bc` + remote catalog seed; user iterative UX approval on device/local.
- **journal:** 2026-07-18 — plan approved; Slice 0+1 S-MA-031; `review` for deploy/device. Residual S-MA-032 mixed not shipped. 2026-07-19 — media/VO/rest/done UX loop + prod; user: close T02 (`review -> done`). S-MA-032 remains deferred residual (not blocking).

### E2-D3-T06 — Seed ready-made exercise media (user square MP4 pack)
- **status:** done · **priority:** high · **owner:** AI agent · **updated:** 2026-07-19
- **prd_refs:** §12.1, §14.2–14.4, §43.6–43.10, §55.2 · **depends_on:** E2-D3-T02 · **decisions:** DEC-016, DEC-029, DEC-053, DEC-062
- **ui_slices:** regression S-MA-031 step media.
- **scope:** user-supplied square MP4 pack (46) → Flowly step catalog exercises + workouts; covers for new workouts; local+prod seed; step UI white 16:9 contain. Separate from E2-D3-T03 (UGC uploads).
- **acceptance:**
  - [x] user source: own `.temp/assets` square MP4 pack (not Gym Visual scrape);
  - [x] full catalog rebuild: 46 video exercises, 21 step workouts + 3 YouTube; pilot then full;
  - [x] media in `public/media/catalog/exercises/*.mp4` + covers; prod deploy + remote D1 seed;
  - [x] step session shows video without object-cover clipping (contain + white letterbox);
  - [x] seed/docs/journal record pack path;
  - [x] commit/push on explicit user command (`e5b55bc`).
- **validation/evidence:** local+prod D1 `media_type=video` count 46; `wo-twists-15`/`wo-standing-18` covers; commit `e5b55bc` Deploy web PASS; remote seed 0002/0003/0006.
- **journal:** 2026-07-18 — card created. 2026-07-19 — user MP4 pack; pilot approved; full catalog + prod; user: close (`backlog/work -> done`).

### E2-D3-T03 — Реализовать собственные тренировки и media uploads
- **status:** blocked · **priority:** low · **owner:** unassigned · **updated:** 2026-07-19
- **prd_refs:** §13.2, §16, §43.7–43.11, §44.3–44.4, §44.13, §46 · **depends_on:** E1-D1-T05, E2-D2-T03 · **decisions:** DEC-016, DEC-019, DEC-022, DEC-024, DEC-025, DEC-029, DEC-035, DEC-064
- **ui_slices:** S-MA-040, S-MA-041, S-MA-042, S-MA-043, S-MA-045 — выполнять последовательно; approval каждого ID обязателен до следующего.
- **scope:** private CRUD, упражнения, изображения/GIF через безопасный R2 flow.
- **blocked_reason:** product deprioritized for current v1 path — Flowly catalog + YouTube + step sessions cover core use; own-workout constructor / R2 upload not needed until demand. Resume only on explicit user request.
- **acceptance:** [ ] ownership обязателен; [ ] ограничения загрузки соблюдены; [ ] удаление/доступ корректны; [ ] публичность не включается скрыто.
- **validation/evidence:** CRUD/upload cases и permission failures.
- **journal:** 2026-07-19 — user: взять в работу. `backlog -> in_progress`. Analysis started. Same day user: functionality not needed for now («может когда-нибудь потом»). `in_progress -> blocked` (product defer, DEC-064). No implementation.

### E2-D3-T05 — Закрыть DoD этапа «Йога»
- **status:** done · **priority:** blocker · **owner:** AI agent · **updated:** 2026-07-19
- **prd_refs:** §50, §55.2, §57 · **depends_on:** E2-D2-T01–T05, E2-D3-T01–T02, E2-D3-T06 · **decisions:** DEC-003, DEC-011, DEC-015, DEC-016, DEC-019, DEC-021, DEC-022, DEC-029, DEC-064, DEC-065
- **scope:** проверить критерии §55.2 и инварианты §57, **релевантные stage 2**. **N/A / deferred:** own-workout constructor (DEC-064 / T03); **ручная запись §17** — E6-D7-T09 (DEC-065), не блокирует stage-2 DoD.
- **acceptance:** [x] каждый применимый критерий имеет evidence (или explicit N/A + DEC); [x] privacy/security проверены; [x] риски YouTube записаны.
- **validation/evidence:** `docs/roadmap/evidence/E2-D3-T05-dod.md`; linked task evidence (catalog/session/step/favorites commits); N/A DEC-064/065.
- **journal:** 2026-07-19 — DEC-064/065 N/A paths. Same day user: close E2-D2-T05 + E2-D3-T05 DoD as done after favorites `fad6bc2`. Checklist written; residual S-MA-032 / T03 blocked documented.

## Handoff этапа

**Stage-2 DoD closed** (E2-D3-T05 `done`). **E2-D3-T03** remains `blocked` (DEC-064) — does not reopen stage DoD. Residual optional: **S-MA-032** mixed chooser. Manual log → E6-D7-T09.
